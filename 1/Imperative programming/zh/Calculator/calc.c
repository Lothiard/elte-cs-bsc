#include "calc.h"

void digit(int n) {
    int input *= 10 + n;
    if(state == left_plus) {
        state = plus_right;
    } else {
        if (state == left_times) {
            state = times_right;
    } else {
        
    }
}
