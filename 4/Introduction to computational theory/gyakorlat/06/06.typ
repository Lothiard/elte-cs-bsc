= veges determinisztikus automatak
$
  A = (Q, Sigma, delta, q_0, F)\
$
- $Q:$ allapotok halmaza, $1 <= abs(Q) < +oo$
- $Sigma:$ input ABC
- $q_0:$ kezdoallapot, $q_0 in Q$
- $F:$ elfogado/vegallapotok, $F <= Q$
- $delta:$ atmenet fuggveny, $delta: Q times Sigma arrow Q$

