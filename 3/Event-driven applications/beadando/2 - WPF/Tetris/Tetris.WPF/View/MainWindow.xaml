<Window x:Class="Tetris.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tetris.WPF"
        mc:Ignorable="d"
        Title="Tetris" 
        Height="675" 
        Width="575"
        ResizeMode="NoResize">
    
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="18"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Game Board Panel -->
        <Border Grid.Column="0" 
                BorderBrush="Black" 
                BorderThickness="1"
                Background="White"
                Height="600"
                VerticalAlignment="Top">
            <Canvas x:Name="GameCanvas" 
                    Width="300" 
                    Height="600"
                    ClipToBounds="True"/>
        </Border>

        <!-- Control Panel -->
        <StackPanel Grid.Column="2" 
                    Width="200"
                    VerticalAlignment="Top">
            
            <!-- Board Size Selection -->
            <Label Content="Pályaméret:" 
                   Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbBoardSize"
                      Height="25"
                      Margin="0,0,0,15">
                <ComboBoxItem Content="4×16"/>
                <ComboBoxItem Content="8×16" IsSelected="True"/>
                <ComboBoxItem Content="12×16"/>
            </ComboBox>

            <!-- New Game Button -->
            <Button x:Name="btnNewGame"
                    Content="Új játék"
                    Height="30"
                    Margin="0,0,0,20"/>

            <!-- Pause Button -->
            <Button x:Name="btnPause"
                    Content="Szünet"
                    Height="30"
                    Margin="0,0,0,20"/>

            <!-- Save Button -->
            <Button x:Name="btnSave"
                    Content="Mentés"
                    Height="30"
                    Margin="0,0,0,20"
                    IsEnabled="False"/>

            <!-- Load Button -->
            <Button x:Name="btnLoad"
                    Content="Betöltés"
                    Height="30"
                    Margin="0,0,0,50"/>

            <!-- Game Time Display -->
            <StackPanel Orientation="Horizontal" 
                        Margin="0,0,0,10">
                <Label Content="Játékidő:" 
                       Width="70"/>
                <Label x:Name="lblTimeValue" 
                       Content="00:00"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
