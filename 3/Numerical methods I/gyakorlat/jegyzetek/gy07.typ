== 3
$
    norm(A)_m := n dot max_(i, j) abs(a_(i j))\
    3. norm(lambda A)_m = n dot max_(i,j) abs(lambda a_(i,j)) = n dot max_(i, j) (abs(lambda) dot abs(a_(i j))) = abs(lambda) dot n dot underbrace(max(a_(i j)), norm(A)_m)
$

spektralnorma csak olyan mint a szaron van lesz zhn

nem kellett ehhez jegyzet minden tokeletesen le van irva most a pdfben

